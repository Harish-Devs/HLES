Git init to initialize repository as it will tell git this file you can consider for pushing


to  make a repo
create a file then  stage it
  git add .
git commit -m "Commit message"
git branch -M main                             (you are in master to rename the  master to main  using m for renaming)
git checkout -b feature/feature_name           (to create a new branch)
git remote add origin https://github.com/Harish-Devs/HLES.git    (add the remote repo link)
gite remote -v                                  (to verify the remote repository linked/mapped)
git push -u origin/main                        (here main is the branch name if u different branch we can use that branch name instead of main  but origin to 						be common since that will point the remote workspace)(u  will permanently map the repo if not we need to 						specify the repo each time  by doing this local-> main will point to remote->main)

ls -R                                          (to see the changes or folder  )
git log --oneline                                (to see the log message on the console)
git log  --oneline --graph --all                 (to see all of them in the graphical format)

git rebase main                                  GO TO FEATURE AND U want latest chanegs from main then use rebase for pushing u r changes on top of main                    
                                                  it can be applicable to feature to feature branch as well
git reset --hard HEAD~1                         undo last commit done i local this will create a new commit for inversing a commmit
git rebase -i                                    udno last rebase activity
git revert --no-commit <hash>                     undo last commit form rrmoete withou adin a commit

git cherry-pick hass ID                           use dto take  aparticular comit and will create a new commit locally
git cherry-pick  --abort
git cherry-pick  -- continue

git fetch                                          just take the latest origin main but keep ur local main as it 
git pull                                             do fetch and merge
git merge                                            after fetch need to do so that it will create a local commit fo origin main and ur main together

git stash                                            acts as a temperory storage
git stash pop                                          take the data back and remov
git stash list                                        see the lsit fo tash
git stash apply                                     wihtou removing the stash
git stash drop stash@{0}                            to delete it



adding this for nested branch parent
adding this for child branch parent

adding thi in the merged parent as a commit to see what happens
we can branch and commit and commit and branch vice versa as branch will point to the lates commit

